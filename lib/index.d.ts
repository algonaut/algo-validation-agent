export interface Props {
    x: number;
    y: number;
}
/**
 * Validator class
 */
export declare class Validator {
    private value;
    private props;
    getValue(): number;
    setValue(value: number): void;
}
