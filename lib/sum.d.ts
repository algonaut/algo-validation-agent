/**
 * Testing a description
 * @category Assets
 * @param {number}
 * @param {number}
 * @returns {boolean}
 * @example
 *
 * // Add two numbers
 * sum(2, 3)
 *
 * if (isError(elements)) {
 *   elements = []
 * }
 */
export declare function sum(a: number, b: number): number;
