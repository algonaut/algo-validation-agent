/**
 * Testing a description
 * @category Assets
 * @param {number}
 * @param {number}
 * @returns {boolean}
 * @example
 *
 * // Add two numbers
 * sum(2, 3)
 *
 * if (isError(elements)) {
 *   elements = []
 * }
 */
export function sum(a: number, b: number) {
  return a + b;
}
